#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - program entry
 *
 * Return: 0 Alway (Success)
 */

int main(void)
{
	int a;

	for (a = '0'; a <= '9'; a++)
		putchar(a);
	putchar('\n');

	return (0);
}
