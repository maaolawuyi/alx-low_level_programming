#include "main.h"

/**
 * swap_int - a function that sawp the value of two integers.
 * @a: An input inteher pointer
 * @b: An input integer pointer
 * Return: Nothing
 */

void swap_int(int *a, int *b)
{
	int sw;

	*sw = *a;
	*a = *b;
	*b = sw;
}
